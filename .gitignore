# OpenSCAD render outputs (generated by CI — don't commit manually)
*.stl
*.amf
*.3mf
*.off
*.dxf
*.svg
*.csg

# Local preview images (CI commits these from renders/ dir)
*.png
*.echo

# OpenSCAD cache
.openscad_cache/

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Allow the renders/ directory (committed by CI bot)
!renders/
!renders/*.png
!renders/*.stl
